{% set route = app.request.attributes.get('_route') %}
{% set route_parameters = app.request.query.all|unset_elements(['order_by', 'sort', 'page', 'limit']) %}
{% set slug = app.request.get('slug') %}

<div class="dropdown-menu dropdown-menu-end ">
    {% for limit in bitbag_es_pagination_available_page_limits %}
        <a class="dropdown-item" href="{{ path(route, route_parameters|merge({'slug': slug, 'limit': limit})) }}">{{ limit }}</a>
    {% endfor %}
</div>
